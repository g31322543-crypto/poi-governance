# POI 决策治理平台 · 完整设计方案

> 定位：从「能跑通的最小 demo」到「可规模化运转的完整治理平台」的设计蓝图。
> 配套：README.md（当前 demo 说明）· 本文档（目标形态）

## 1. 背景与问题

### 1.1 业务背景
TikTok Local Services 维护着海量 POI（兴趣点，即「店」）。POI 是本地生活服务的地基——用户能否搜到/找到对的店、商家能否被正确曝光，直接取决于 POI 数据质量。

### 1.2 表面痛点：两类高频、高耗人的判断
- **POI 判重**：两条记录是不是同一家店？（是 → 合并；否 → 各自保留）
- **审核预筛**：商家新提交的一条 POI 该不该放行？（真店 → 过；垃圾/诈骗 → 拒）

这两件事过去靠人一条条看，量大、重复、成本高。

### 1.3 深层痛点：POI 数据本身的脏与复杂
治理的难点 90% 不在「判断算法」，而在数据本身。按数据质量六维拆解：

| 维度 | 典型表现 |
|---|---|
| 完整性 | 字段大量缺失：坐标、电话、分类、营业时间常为空 |
| 准确性 | 坐标漂移、坐标系不统一（GCJ-02 / BD-09 / WGS-84）、电话占位（13800000000）、地址口语化 |
| 唯一性 | 同一家店被多源录成多条；名字微漂移、多语言/音译重复 |
| 时效性 | 店关门/搬迁/改名后 POI 仍挂着（幽灵 POI），数据只旧不新 |
| 一致性 | 多源字段互相冲突，无权威源 |
| 语义复杂度 | 「同一家店」边界模糊（商场 vs 店中店、档口、连锁加盟）；长尾小店极度稀疏 |

## 2. 设计原则

1. **用决策模型，不用通用大模型**：这是高频小判断而非内容生成，要的是「倾向 + 置信度」两个结构化输出，不是长文本。
2. **分级，而不是全自动**：全自动必然带错落地（把两家店合并错、把垃圾店放进来都是真损失）；分级让高置信自动、低置信交人。
3. **证据与判断分离**：能算的（坐标换算、距离、名称相似度、品牌归一）在代码里算好当证据，模型只「综合证据下判断」，不负责算数。
4. **全量存储、按场景取用**：入库存全字段，喂模型只挑与该判断相关的证据字段。

## 3. 完整架构（六层）

```
① 数据接入层
   商家提交 + 存量POI库 + 第三方/地图供应商 + 用户UGC
   → 清洗 / 坐标系统一 / 多语言归一 / 去噪
              │
              ▼
② 候选生成 + 特征层
   分块(blocking)生成「候选对」→ 算好特征当证据
   （距离、名称相似度、品牌、分类、电话匹配）
              │
              ▼
③ 决策引擎（分级漏斗）
   规则硬筛 → 决策模型打分 → 置信度路由
   ┌──────────┬──────────┬───────────┐
   │ 自动处理 │ 升级人工 │ 待补全/异常 │
   └──────────┴──────────┴───────────┘
              │
              ▼
④ 人工审核 + 结果落地
   审核队列 → 人审 → 合并/拒绝/通过 写回POI库
              │
              ▼
⑤ 评测 + 反馈闭环
   评测集 → 指标 → 阈值标定 → 人工反馈回流调优
              ▲
              └────────────────────────┘
⑥ 平台底座（横切）：任务调度 / 监控告警 / 成本核算 / 权限
```

各层职责：

| 层 | 职责 | 主要解决哪些痛点 |
|---|---|---|
| ① 数据接入 | 多源接入、清洗、坐标系统一、多语言归一 | 准确性（坐标）、一致性（归一） |
| ② 候选+特征 | 分块降 N²、特征下沉 | 唯一性（判重可行）、准确性 |
| ③ 决策引擎 | 分级漏斗、规则+模型+路由 | 唯一性、语义复杂度 |
| ④ 人工+落地 | 兜底、写回 | 语义复杂度、时效性 |
| ⑤ 评测闭环 | 指标、阈值标定、回流 | 一致性、准确性 |
| ⑥ 平台底座 | 调度、监控、成本 | 规模化运维 |

## 4. 核心机制：置信度分级漏斗

每条样本只问模型一个 score 问题（5 级有序 rubric），一次拿到「倾向 + 置信度」：

```
         决策模型（倾向 + 置信度）
              │
   ┌──────────┴──────────┐
   │                     │
 信心高、方向明确       信心低、模棱两可
   │                     │
   ↓                     ↓
 自动处理             升级人工
```

设计原则：模型只对自己有把握的样本自动决策，没把握的不装懂、交给人。

## 5. 业务场景闭环（六个场景）

扩到完整治理闭环，覆盖 JD 五个场景 + 一个横切能力：

| # | 场景 | 输入 | 输出 | 对应痛点 | 状态 |
|---|---|---|---|---|---|
| 1 | 属性补全 | 字段缺失的 POI | 补全的坐标/分类/电话/营业时间 | 完整性 | 待建 |
| 2 | POI 判重 | 候选对 | 合并/保留/升级 | 唯一性 | 已有 |
| 3 | 审核预筛 | 商家提交 | 通过/拒绝/升级 | 准确性 | 已有 |
| 4 | 关闭/变更检测 | 存量 POI + 信号 | 疑似关闭清单 | 时效性 | 待建 |
| 5 | 异常数据挖掘 | 全量 POI | 风险打分排序 | 一致性/准确性 | 待建 |
| 6 | 多语言归一 | 跨语言名称/地址 | 归一后的实体 | 唯一性（横切） | 待建 |

**场景间互相咬合**：补全(1) 喂判重(2)，判重(2) 后仍解决不了的进关闭检测(4) 与异常挖掘(5)；多语言归一(6) 是前几者的横切能力。

## 6. 数据治理底座：来源可信度 + 质量分

为解决一致性痛点，所有字段带「来源 + 质量分」：

- **字段级可信度**：商家自报 > 地图供应商 > 用户 UGC
- **冲突仲裁**：按可信度融合，而非简单覆盖
- **分块/特征优先用高质量来源字段**，避免脏字段污染干净字段

## 7. 批量处理管道

```
定时/触发 → 读增量数据 → 分块生成候选对 → 批量打分（并发+限流+重试+降级）
         → 自动决策 + 生成人工审核队列 → 结果写库 → 出报表
```

- **离线批量**：量大、可慢（T+1 全量/增量）
- **在线实时**：商家提交即预筛（量小、延迟敏感）
- **候选对生成（分块）**：判重不能两两全比（N² 爆炸）；用城市/geo 网格/品牌/名称前缀分块，只比「可能相同」的。这是「可上量」的分水岭。

## 8. 评测与反馈闭环

- 评测集带隐藏 ground truth，算：自动覆盖率、自动准确率、人工升级率、成本
- **阈值用数据标定**，而非写死；判重/审核各用各的阈值
- **置信度校准检验**（reliability diagram / ECE）
- **非对称成本**：false merge（错合并）比 false split（错拆分）危害大，评测加权
- **人工反馈回流**：人审结果 → 进评测集 → 检模型退化 → 调阈值/rubric

## 9. 落地里程碑

| 阶段 | 目标 | 内容 |
|---|---|---|
| M1（已完成） | 验证核心假设 | 判重+预筛 demo、分级漏斗、mock 评测 |
| M2 | 补齐判重前置 | 候选对生成/分块、特征下沉、字段级可信度 |
| M3 | 补成业务闭环 | 人工审核队列、结果落地、属性补全、关闭检测 |
| M4 | 可规模化 | 批量管道、数据接入、监控成本、异常挖掘、多语言归一 |

## 10. 与当前 demo 的差距清单

| 完整项目该有的环节 | demo 现状 | 缺什么 |
|---|---|---|
| 数据接入 | 合成数据一次性生成 | 真实数据批量/增量接入与清洗 |
| 候选对生成 | 手搓 28 对 | 分块（blocking） |
| 批量调度 | 界面点一个跑一个 | 定时/触发批量管道 |
| 人工审核 | 有个人审接口 | 审核队列 + 回流闭环 |
| 结果落地 | 决策停在内存 | 真正写回 POI 库 |
| 属性补全/关闭检测/异常挖掘 | 占位 | 完整实现 |
